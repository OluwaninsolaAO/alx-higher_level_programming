#!/usr/bin/python3
def multiple_returns(sentence):
    """No add descrptn since: well sentence"""
    if not sentence:
        pass
    else:
        lenght = len(sentence)
        return (lenght, sentence[0])
